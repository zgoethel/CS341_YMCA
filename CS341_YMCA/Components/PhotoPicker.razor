<fieldset>
    <legend>@Label</legend>
    <div class="control-group">
        <img src="@(string.IsNullOrEmpty(previewUri) ? "images/not_found.svg" : previewUri)"
            style="height: 10em;max-width: 100%;" />
        <br />

        <InputFile OnChange=FileSelected class="mt-4" />
    </div>

    <button class="btn btn-primary mt-3" @onclick="(e) => SaveImage()">Test Save</button>
</fieldset>